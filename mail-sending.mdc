---
description: 邮件发送必须走 Player Actor 或中转消息
globs: internal/game-logic/gameserver/**/mail/**/*.go,internal/game-logic/gameserver/**/player/**/*.go,internal/game-logic/gameserver/logic/system/**/*.go
alwaysApply: false
---

# 邮件发送

只有 Player Actor 内可直接 `p.mail.AddMail()`。

其他 Actor 必须通过消息中转到 PlayerSystem：
- `def.MsgId_Actor_Mail_AddGlobalMail` — 全服邮件
- `def.MsgId_Actor_Mail_AddMultiUserMail` — 指定玩家邮件
